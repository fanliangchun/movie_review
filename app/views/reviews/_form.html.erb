<%= simple_form_for([@movie, @movie.reviews.build]) do |f|%>
	<div class="ratyrate">
		<p>
		<%= @movie.title %> 
		<span style="display: inline-block;"><%= rating_for @movie, 'color', disable_after_rate: true, cancel: true %></span>
		<span style="display: inline-block;"><%= rating_for @movie, 'taste', disable_after_rate: true, cancel: true %></span><br>
		<span style="display: inline-block;"><%= rating_for @movie, 'shape', disable_after_rate: true, cancel: true %></span>
		</p>
	</div>
  <div class="field">
      <%= f.text_area :comment, class: "form-control" %>
  </div><br>

  <%= f.submit "发表影评", class: "btn btn-primary" %>
<% end %>